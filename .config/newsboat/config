auto-reload yes
reload-threads 20
browser "xdg-open %u"
macro m set browser "setsid -f mpv --really-quiet --no-terminal"; open-in-browser; set browser "xdg-open %u"
text-width 80

# vim binds
bind-key h quit
bind-key BACKSPACE quit
bind-key j down
bind-key k up
bind-key l open
bind-key H prev-feed
bind-key L next-feed
