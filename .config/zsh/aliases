alias cat="bat"
alias cp="cp -i"
alias ele="im.riot.Riot --enable-features=UseOzonePlatform --ozone-platform=wayland; exit"
alias g="cd ~/.local/repos"
alias ga="git commit -a; git push -vvv"
alias gc="git commit -a"
alias gd="git diff | bat -p"
alias gl="clear; git log --oneline"
alias gpull="git pull -vvv"
alias gpush="git push -vvv"
alias l="exa"
alias ls="exa -l"
alias mv="mv -i"
alias nano="$EDITOR"
alias nd="nmcli device"
alias pa="pactl set-sink-volume @DEFAULT_SINK@ 100%"
alias rm="rm -i"
alias sig="org.signal.Signal --enable-features=UseOzonePlatform --ozone-platform=wayland; exit"
alias t="cd /run/media/polarhive/WD\ Silver\ LIVE\ TECH\ 1T\ lenovo/backups/Nathan/cider"
alias v="$EDITOR"
alias vim="$EDITOR"
alias wf="nmcli radio wifi off; notify-send wifi off; exit"
alias wo="nmcli radio wifi on; notify-send wifi on; exit"
alias z="exit"
